// The canonical production D1 is the existing data-bearing database that was
// originally provisioned under this legacy Cloudflare resource name. The name
// is retained to avoid data movement; the protected UUID is the authoritative
// release identity.
export const PRODUCTION_D1_RESOURCE_NAME = "rcat-public-api-preview";
