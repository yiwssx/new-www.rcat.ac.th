// Deprecated: public pages now read institution profile text from CmsSnapshot.siteSettings.
const textElementSettings = {
  home: "หน้าแรก",
  staffLogin: "สำหรับเจ้าหน้าที่",
  backToTop: "กลับขึ้นด้านบน"
} as const;

export default textElementSettings;
