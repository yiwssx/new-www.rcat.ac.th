# Conservative restore behavior

Deleted content is not republished automatically. Restore recovers the canonical pre-delete slug from the revision snapshot, checks for an active slug collision, clears the soft-delete marker, and returns the item as Draft for editorial review.
